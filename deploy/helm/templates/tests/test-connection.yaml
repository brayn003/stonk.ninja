apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stonk-ninja.fullname" . }}-test-connection"
  labels:
    {{- include "stonk-ninja.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stonk-ninja.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
