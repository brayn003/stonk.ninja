apiVersion: v1
kind: Service
metadata:
  name: {{ include "stonk-ninja.fullname" . }}
  labels:
    {{- include "stonk-ninja.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stonk-ninja.selectorLabels" . | nindent 4 }}
